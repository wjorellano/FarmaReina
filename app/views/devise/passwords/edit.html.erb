<div class="container mt-5">
  <div class="row mt-5">
    <div class="col-md-4 mx-auto mt-5  rounded">
      <h3 align="center" class="mt-2">Cambiar contraseña</h3>
      <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
        <div class="form-inputs p-5">
          <%= f.input :password,
                      label: "Nueva contraseña",
                      required: true,
                      autofocus: true,
                      hint: ("#{@minimum_password_length} caracteres minimo" if @minimum_password_length),
                      input_html: { autocomplete: "new-password" } %>
          <%= f.input :password_confirmation,
                      label: "Confirmar contraseña",
                      required: true,
                      input_html: { autocomplete: "new-password" } %>
          <div class="form-actions">
            <%= f.button :submit, "Actualizar", class:"btn btn-lg btn-success btn-block" %>
          </div>
          <div class="row mt-4">
            <div class="col"><p align="center"><%= link_to "Iniciar sesión" , new_session_path(resource_name) %></p></div>
            <div class="col"><p align="center"><%= link_to "Crear cuenta" , new_registration_path(resource_name) %></p></div>
          </div>
        </div>
        <% end %>
    </div>
  </div>
</div>